# type Mutation {
#   addBooking(ownerId: ID!, bookingInput: BookingInput!): Booking
# }

type Mutation {
  addBooking(property: ID!, user: ID!, message: String): Booking
}

type Subscription {
  bookingAdded(ownerId: ID!): Booking
  userRegistered: User!
}

input BookingInput {
  property: ID!
  user: ID!
  message: String
  status: String
}
